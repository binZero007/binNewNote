正向代理： 通过浏览器配置代理服务器  通过它访问互联网

反向代理：隐藏真正的服务器   以代理服务器作为中间站 进行代理

负载均衡 

ps -ef | grep nginx 

ps：显示某某进程     |  管道命令，表示前面和后面命令同时进行   grep 查找，能适用正则表达式查找
-e  显示所有
-f  全格式


vi  是打开某个文件
进入打开的文件后 处于命令模式  可以用  i  或者  a 命令进入编辑模式
要保存修改  则按esc返回命令模式。
输入冒号（英文）切换到最后一行。
按x保存编辑的文件
也可以按wq保存
q! 退出不保存